repos:
  # ---------------------------------------------------------------------------
  # Ruff — lint and format
  # ---------------------------------------------------------------------------
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.15.9
    hooks:
      - id: ruff-check
        name: ruff lint
        types_or: [python, pyi, jupyter, pyproject]
        args: [--fix]
      - id: ruff-format
        name: ruff format

  # ---------------------------------------------------------------------------
  # Local hooks — ty, pytest, CLI smoke tests
  # Requires: uv sync --group dev
  # ---------------------------------------------------------------------------
  - repo: local
    hooks:
      - id: ty
        name: ty type check
        entry: uv run ty check channel_ten
        language: system
        pass_filenames: false
        always_run: true
        types_or: [python, pyi]

      - id: pytest
        name: pytest
        entry: uv run pytest
        language: system
        pass_filenames: false
        always_run: true
        types_or: [python, pyi]

      # CLI smoke tests — verify each subcommand loads and parses args cleanly
      - id: cli-smoke-scrape
        name: "cli smoke: scrape --help"
        entry: uv run python -m channel_ten.cli
        args: [scrape, --help]
        language: system
        pass_filenames: false
        always_run: true

      - id: cli-smoke-import
        name: "cli smoke: import --help"
        entry: uv run python -m channel_ten.cli
        args: [import, --help]
        language: system
        pass_filenames: false
        always_run: true

      - id: cli-smoke-parse
        name: "cli smoke: parse --help"
        entry: uv run python -m channel_ten.cli
        args: [parse, --help]
        language: system
        pass_filenames: false
        always_run: true

      - id: cli-smoke-publish
        name: "cli smoke: publish --help"
        entry: uv run python -m channel_ten.cli
        args: [publish, --help]
        language: system
        pass_filenames: false
        always_run: true

      - id: cli-smoke-validate
        name: "cli smoke: validate --help"
        entry: uv run python -m channel_ten.cli
        args: [validate, --help]
        language: system
        pass_filenames: false
        always_run: true
